#include <stdio.h>

int main(){

    int a;
    char b;
    float c;
    double d;

    printf("The Size for a is %d\n", sizeof(a));
        printf("The Size for int is %d\n", sizeof(int));

    printf("The Size for b is %c\n", sizeof(b));
        printf("The Size for char is %c\n", sizeof(char));

    printf("The Size for float is %f\n", sizeof(c));
    printf("The Size for double is %lf", sizeof(d));

    //Complete the code later after lecture

    return 0;

}